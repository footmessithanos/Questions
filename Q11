#include <stdio.h>

int main() {
    int A,B;
    int C,D;
    scanf("%d%d",&A,&B);
    scanf("%d%d",&C,&D);
    if(C>=A&&D>=B) {
        printf("POSSIBLE");
    } else {
        printf("IMPOSSIBLE");
    }

}
